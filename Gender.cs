using System;

namespace Labb4
{
    // Create and allocate an enum for gender and set values for easy input when adding a new Person 
    public enum Gender
    {
        
        Man = 0, 
        Woman = 1, 
        NonBinary = 2, 
        Other = 3

    }
}
